/*
 * This file is used to load modules that are needed
 * for the flow called {{flowName}}
 */

YUI({
  modules: {
    '{{flowName}}': {
      lang: ['en'],
      requires: [
        'base-build'
      ]
    }
  }
}).use('{{flowName}}', function (Y) {
  // ready to use {{flowName}}
  Y.log('ready...');
});
