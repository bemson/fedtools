/**
 * Provides the `{{namespace}}.{{ClassName}}` module.
 *
 * @param config {Object} Object literal specifying SimpleMenu configuration properties.
 *
 * @class {{ClassName}}
 * @constructor
 * @extends {Widget}
 * @module {{name}}
 * @namespace {{namespace}}
 */

Y.namespace('{{namespace}}').{{ClassName}} = Y.Base.create(
  '{{baseName}}', Y.Widget, [], {
    initializer: function (config) {

    },
    bindUI: function () {

    },
    syncUI: function () {

    },
    destructor: function () {

    }
  }, {
    CSS_PREFIX: CSS_PREFIX,
    ATTRS: {

    },
    HTML_PARSER: {

    }
  }
});
